README.md:
	echo "# Guessing Game" > README.md
	echo "Make was run on `date`" >> README.md
	echo "The number of lines of code in guessinggame.sh is `wc -l guessinggame.sh | cut -d' ' -f1`" >> README.md
